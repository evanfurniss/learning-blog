const router = require("express").Router();
const { get } = require("mongoose");
// const teamController = require("../../controllers/indexController");
// gotta change the name to match new controller name



module.exports = router;